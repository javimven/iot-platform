# Estado general del proyecto

Leyenda de estados: `Pendiente` · `En análisis` · `Decidido` · `En desarrollo` · `Bloqueado` · `Terminado`

Esta tabla se actualiza al cierre de cada etapa. No sustituye a los documentos de cada etapa, solo da una vista rápida del avance global.

> **Nota de terminología:** las "etapas" de esta tabla son etapas del **proceso de diseño/construcción** (descubrimiento, requisitos, arquitectura...), numeradas 0-15. Son distintas de las **fases de producto** (MVP / V2 / Futuro) usadas en `docs/PRODUCT_REQUIREMENTS.md` para decidir qué función se entrega cuándo. Un mismo módulo (p. ej. Alertas) puede tener su etapa 1 (requisitos funcionales) ya cerrada mientras parte de su alcance queda diferido a la fase de producto V2.

| # | Etapa del proceso | Estado | Notas |
|---|------|--------|-------|
| 0 | Descubrimiento y definición de producto | **Decidido** | Sector: agro/ambiental. Alta de orgs: manual (admin de plataforma). Modelo: SaaS multi-tenant compartido. Equipo: 3-6 personas, unos meses. |
| 1 | Requisitos funcionales detallados | En análisis | Borrador en `docs/FUNCTIONAL_REQUIREMENTS.md`, pendiente de tu validación final |
| 2 | Requisitos no funcionales (valores medibles) | En análisis | Recalculado con datos reales (4 sensores/estación, 15-30 min, JSON). Pendiente de validación final |
| 3 | Arquitectura | En análisis | Borrador en `docs/ARCHITECTURE.md` + 3 ADR en `docs/ADR/`, pendiente de tu validación |
| 4 | Modelo de permisos (RBAC detallado) | En análisis | Borrador en `docs/PERMISSIONS.md` (incluye alcance por instalación), pendiente de tu validación |
| 5 | Modelo de datos | En análisis | Borrador en `docs/DATA_MODEL.md`. Corrigió Etapa 3 (solo el gateway se autentica, no el dispositivo) y simplificó Etapa 2 (retención sin export a S3) |
| 6 | Contratos MQTT | En análisis | Borrador en `docs/MQTT_PROTOCOL.md`: topics, payload JSON, QoS 1, detección offline (LWT + timeout 2,5×), creación automática de canales |
| 7 | Contrato de API (OpenAPI) | En análisis | Borrador en `docs/API_DESIGN.md` + `docs/OPENAPI.yaml` (43 rutas, 36 esquemas, validado sintácticamente) |
| 8 | Seguridad y modelo de amenazas | En análisis | Borrador en `docs/SECURITY.md` + `docs/THREAT_MODEL.md`. Corrigió un vector de inyección SQL en el mecanismo de RLS (`DATA_MODEL.md`) |
| 9 | Infraestructura y DevOps | En análisis | Borrador en `docs/DEPLOYMENT.md` + `docker-compose.yml` + `infra/docker/Dockerfile` + `.github/workflows/ci-cd.yml` (validados). Proveedor: Hetzner + DO Managed Postgres + Cloudflare |
| 10 | Observabilidad | En análisis | Borrador en `docs/OBSERVABILITY.md`. Grafana Cloud + UptimeRobot + Healthchecks.io + Crashlytics. Collector activado en `docker-compose.yml` |
| 11 | Estrategia de pruebas | En análisis | Borrador en `docs/TESTING_STRATEGY.md` + `.github/workflows/scheduled-load-resilience.yml` (validado). Consolida las pruebas ya definidas en Etapas 1-10 |
| 12 | Backup y recuperación / continuidad | En análisis | Borrador en `docs/BACKUP_AND_RECOVERY.md` + `docs/INCIDENT_RESPONSE.md`. Backup en 2 capas (DO + Hetzner), restauración selectiva por organización, 4 niveles de severidad |
| 13 | Desarrollo backend (por módulo) | **Terminado y validado contra infraestructura real** | IAM/Auth, Directorio IoT, Telemetría/Ingestión/Alertas, Organización/Miembros/Plataforma/Auditoría, Notificaciones por email. 46 tests unitarios + 4 de integración (nuevo, contra Postgres real) en verde. **2026-07-28: primera vez que este proyecto corrió contra Docker/Postgres/Redis/EMQX/Mailpit reales** (Docker Desktop instalado en este entorno) — encontró y corrigió 3 gaps críticos invisibles hasta entonces: (1) faltaba la migración `0001_init` (nunca se había podido generar sin un Postgres vivo); (2) el rol de conexión de la app era el mismo superusuario dueño de las tablas — **RLS estaba completamente sin efecto**, dos organizaciones de prueba se veían mutuamente todos sus datos; (3) incluso con el rol correcto, `set_config(x, NULL, true)` no guarda `NULL` (Postgres lo convierte en `''`), y `''::uuid` rompía con 500 cualquier login — corregido con `NULLIF(...)` en las 9 políticas RLS afectadas. Los tres fixes verificados end-to-end con dos organizaciones reales (login, invitación por email vía Mailpit, aislamiento confirmado, acceso cruzado → 404). Detalle completo en `DATA_MODEL.md` §7/§8/§9 |
| 14 | Desarrollo frontend Flutter | En desarrollo | `apps/mobile`: login, **activar cuenta invitada / recuperar y restablecer contraseña**, **gestión de miembros** (listar, invitar, cambiar rol/estado, asignar alcance por instalación, eliminar), **sesiones activas propias** (listar, cerrar sesión en otro dispositivo), **ajustes de organización** (perfil, funciones contratadas, umbrales por defecto de canal), **auditoría de organización**, y **panel de plataforma** (organizaciones: listar/crear/suspender/reactivar; editor de funciones contratadas; auditoría de plataforma — nuevo, ver más abajo) para el Admin de plataforma "puro", instalaciones (lista+últimas lecturas), gráfica histórica por canal (24h/7d/30d, `fl_chart`), alertas (filtro por estado, reconocer/resolver), y **Directorio IoT completo** (zonas: alta/edición/baja; gateways: alta/edición/deshabilitar + credencial mostrada una vez; dispositivos: alta/edición [incl. reasignar zona]/deshabilitar; sensores: alta/baja; canales: edición de umbral). Validado localmente con Flutter 3.44.8 estable: `flutter analyze` sin incidencias, `flutter test` 25/25, `flutter build web` y `flutter build apk --debug` compilan ambos. **iOS validado vía GitHub Actions** (`mobile-ios-build.yml`, runner macOS, sin firma de código) — primera ejecución con éxito en ~3 min, [run](https://github.com/javimven/iot-platform/actions/runs/30352818973). Repo publicado en GitHub (`github.com/javimven/iot-platform`, público). **Los tres targets (web, Android, iOS) están validados por primera vez.** |
| 15 | Mantenimiento y operación continua | **Decidido** | Borrador en `docs/MAINTENANCE.md`: consolida cadencias ya fijadas (escaneo de dependencias, rotación de credenciales cada 90 días, simulacros mensuales) en un único calendario, y junta los umbrales de escala dispersos ("revisar si crece 10x") en una tabla única de "cuándo reabrir una decisión". No implementa aún la automatización de recordatorios (Healthchecks.io cron) — queda como tarea de infraestructura viva, pendiente de tu validación |

> Esta es una propuesta de desglose de fases derivada del proceso descrito por el usuario. Se puede fusionar, dividir o reordenar según convenga a medida que avancemos.

## Backlog de ideas
Cualquier funcionalidad mencionada fuera de orden se captura en [`docs/BACKLOG.md`](docs/BACKLOG.md), no aquí. Este archivo solo lleva el estado de las etapas del proceso.

## Cambios retroactivos aplicados
- 2026-07-30: **Etapa 9: escrito el flujo de despliegue real** (`infra/docker/docker-compose.deploy.yml`/`Caddyfile`/`deploy.sh`/`.env.example`) — preguntaste si había algo más que hacer sin las cuentas de proveedor todavía pendientes, y sí: el propio README de Terraform ya señalaba este hueco ("qué corre dentro de cada servidor" no estaba escrito). `api` con 2 réplicas nombradas (despliegue sin interrupciones), `worker`/`ingestion`/`redis`/`emqx` autoalojados, Caddy (reverse proxy + TLS automático + estáticos de la app Flutter web), `otel-collector` reenviando a Grafana Cloud (`infra/otel/otel-collector-prod.yaml`, nuevo).
  - **Al construirlo se encontró y corrigió un bug crítico real, `BACKLOG.md` #22: la imagen Docker `runtime` que `ci-cd.yml` ya construye y publica en cada commit no podía conectar con Postgres en absoluto** — el motor de Prisma se generaba contra la variante equivocada de OpenSSL (`node:20-alpine` trae OpenSSL 3.x; el motor se generaba para 1.1.x porque el CLI `openssl` no estaba instalado para que Prisma detectara la versión real, y esa misma detección se repite en cada arranque del proceso, no solo al generar). Nunca se había detectado porque las pruebas de integración de CI corren en el runner de GitHub Actions directamente, nunca dentro de la imagen construida — la primera vez que se habría probado de verdad habría sido el primer despliegue real. Corregido instalando `openssl` en las tres etapas del Dockerfile que lo necesitan.
  - Verificado en vivo, sin ninguna cuenta externa, solo con Docker local: se reprodujo el fallo exacto (`Error loading shared library libssl.so.1.1`), se corrigió, y se confirmó un login real de principio a fin dentro del contenedor (Argon2 + consulta Postgres real + JWT RS256 + sesión creada) — `HTTP 201` con `accessToken` válido. Suite completa de tests backend (46/46 unitarios, typecheck/lint/format) repetida tras el cambio para confirmar que nada se rompió (no la tocan directamente, corren fuera de Docker).
  - Nueva etapa `migrator` en el Dockerfile (conserva el CLI de `prisma`, que `runtime` quita a propósito) para poder migrar el esquema en el momento del despliegue sin reintroducir herramientas de build en la imagen que sirve tráfico — `ci-cd.yml` ahora también la construye y publica (`:sha-migrator`), cambio puramente aditivo, no toca el build/push de `runtime` ni los pasos de despliegue (que siguen siendo marcadores de posición, sin VPS real todavía donde probarlos).
  - Nuevo `infra/docker/postgres-init/02-grant-app-role-managed.sql` — equivalente del `01-app-role.sql` local para DigitalOcean Managed Postgres (sin `CREATE ROLE`, ese paso ya lo hace `infra/terraform/modules/database`; solo los `GRANT`/`ALTER DEFAULT PRIVILEGES` que Terraform no puede hacer porque las tablas todavía no existen cuando se aplica).
  - Todo validado en local sin ninguna cuenta de proveedor: `docker compose config`, `caddy validate`/`fmt` (instalado Caddy 2.11.4 vía `winget`), `shellcheck` (instalado 0.11.0) — limpio. `DEPLOYMENT.md`/`infra/terraform/README.md` actualizados para reflejar que este hueco ya no existe.
  - **El commit con el fix de #22 pasó limpio en local pero falló Trivy en el primer run real de CI — segundo bug crítico real, `BACKLOG.md` #23.** Sin acceso al log (necesita permisos de admin), se reprodujo descargando la imagen ya publicada en `ghcr.io` (pública) y escaneándola en local: 29 vulnerabilidades CRITICAL/HIGH, todas de paquetes que deberían ser solo devDependencies (`eslint`, `jest`, `typescript`, `@nestjs/cli`...) — 362 MB de `node_modules` en la imagen de CI frente a 180 MB en una build local idéntica, incluso con `--no-cache`.
    - **Primer diagnóstico (equivocado): `npm prune --omit=dev` poco fiable entre entornos.** Se sustituyó por una etapa `prod-deps` dedicada con `npm ci --omit=dev` en vez de "instalar todo y podar después". Verificado en local: 180 MB, Trivy en 0 hallazgos, mismo login real de #22 repetido con éxito. Se dio por resuelto y se hizo commit/push.
    - **El mismo fallo, idéntico, reapareció en el siguiente run de CI** — prueba de que el primer diagnóstico era erróneo, ya que el cambio no había tocado la causa real. Encontrada inspeccionando el `Entrypoint` de la imagen fallida (`docker inspect --format '{{.Config.Entrypoint}}'` → `["npx","prisma","migrate","deploy"]`, y `/app/dist` inexistente): el primer paso `docker/build-push-action@v5` de `ci-cd.yml` (el que publica `image_tag`, la imagen que sirve tráfico) no tenía `target:` explícito — Docker construye por defecto la ÚLTIMA etapa del Dockerfile cuando no se especifica una, y esa última etapa pasó a ser `migrator` (no `runtime`) en cuanto se añadió al final del fichero al corregir #22. Nunca se reprodujo en local porque las pruebas locales siempre usaron `--target runtime` explícito.
    - **Fix real**: `target: runtime` añadido a ese primer paso de `ci-cd.yml`. Reproducido en local de forma determinista (`docker build` sin `--target` → entrypoint de `migrator`; con `--target runtime` → imagen correcta) — no era un misterio de entorno. La etapa `prod-deps` se conserva como endurecimiento razonable, pero no era la causa del fallo real; `docs/BACKLOG.md` #23 corregido para reflejar el diagnóstico verdadero.
    - **Confirmación final en CI real (commit `be4e9a6`, run 30567025184)**: los 14 jobs pasaron, incluido `docker-image`/Trivy. Verificado además fuera de CI: se descargó la imagen recién publicada y se inspeccionó — `Entrypoint` `[node dist/main.js]` (no `migrator`), `/app/dist/main.js` presente, `node_modules` 180.3 MB sin `eslint`/`jest`/`typescript`. Bug #23 cerrado de verdad, no solo "check verde".
- 2026-07-30: **Etapa 9 (Infraestructura): cuentas reales creadas y primer `terraform apply` real contra `dns-zone` y `environments/staging`.** Cuentas: Hetzner Cloud, DigitalOcean, Cloudflare (token de API + Global API Key, ver más abajo), dominio `jmvsoluciones.com` (registrado vía Cloudflare Registrar — a coste, sin margen, con privacidad WHOIS incluida), Brevo (SMTP transaccional, dominio remitente verificado con DKIM/DMARC) y Grafana Cloud (región EU Germany/`eu-central-1`, coherente con Frankfurt ya elegido para la base de datos).
  - **Permiso insuficiente #1 — creación de zona Cloudflare vía token con scopes personalizados.** Ni la combinación de permisos documentada por la propia comunidad de Cloudflare para "crear zona" (`Account: Account Settings: Edit` + `Billing: Edit`) resultó fiable — hay hilos oficiales de gente que la probó y aun así les dio 403. Como `dns-zone` se aplica una sola vez en la vida del proyecto, se optó por usar la **Global API Key** clásica (`CLOUDFLARE_EMAIL`+`CLOUDFLARE_API_KEY`) solo para ese `apply` puntual, dejando un token con scopes normales (`Zone:DNS:Edit`+`Zone:Zone:Read`, restringido a la zona ya creada) para el uso continuo en `environments/*`.
  - **El dominio, al registrarse con Cloudflare Registrar, resultó ya tener una zona creada automáticamente** — el primer `terraform apply` de `dns-zone` falló con "jmvsoluciones.com already exists" (HTTP 400). Corregido con `terraform import cloudflare_zone.this <zone_id>` (Zone ID sacado del dashboard) en vez de crearla — `terraform plan` posterior confirmó "No changes", la zona importada coincide exactamente con la configuración deseada.
  - **Permiso insuficiente #2 — `environments/staging` real.** El primer `apply` completó 7 de 11 recursos (SSH key, firewall, servidor Hetzner, bucket Object Storage, los 3 registros DNS) y falló al crear el cluster de PostgreSQL: "user does not have permission to create tags" (403). El token de DigitalOcean se había creado con Custom Scopes para "Databases" (Create/Read/Update/Delete), pero `tags:create`/`tags:read` es una categoría de permiso separada en la API de DO que no se había incluido — Terraform necesita crearla porque `digitalocean_database_cluster` lleva `tags = ["staging"]`. Se regeneró el token sin Custom Scopes (acceso completo clásico) y se completó el `apply`: los 4 recursos restantes (cluster Postgres, base `iot_platform`, firewall de base de datos, usuario `iot_platform_app`) se crearon sin más incidencias. Decisión registrada en `DEPLOYMENT.md` §19: para este proyecto, tokens de acceso completo en vez de perseguir Custom Scopes granulares cada vez que Terraform toca un recurso nuevo.
  - **Estado real resultante, verificado con los outputs de Terraform** (no solo "apply exitoso"): servidor Hetzner en `2.28.10.232`, DNS ya resolviendo (`api`/`app`/`mqtt`.`staging.jmvsoluciones.com`), cluster Postgres gestionado en DigitalOcean (Frankfurt) con la base y el usuario restringido ya creados, bucket `iot-platform-staging` en Hetzner Object Storage. 11 recursos en total, 0 destruidos.
  - **Gestión de secretos durante todo este proceso**: se creó `infra/terraform/secrets.env` (Bash) y `secrets.ps1` (PowerShell) — ambos añadidos a `.gitignore`, nunca pegados en el chat, cargados con `source`/`. .\` justo antes de cada comando de Terraform. Un desliz propio real: al depurar por qué `secrets.ps1` no cargaba las variables, se usó `xxd` para inspeccionar la codificación del archivo, lo que imprimió el valor real de `HCLOUD_TOKEN` en la conversación — se recomendó y asumió como hecha la rotación de ese token en Hetzner. También se detectó (antes de que llegara a Git) que las credenciales de Object Storage se habían pegado por error en `terraform.tfvars.example` (versionado) en vez de `terraform.tfvars` (ignorado) — corregido antes de cualquier `git add`/commit, nunca llegó a subirse.
  - `terraform apply` en sí quedó bloqueado para que lo ejecute el propio Claude Code (el clasificador de modo automático lo bloquea por defecto para cambios reales de infraestructura) — cada `apply` real de esta sesión lo ejecutó el usuario directamente en su propia terminal, con `init`/`plan` preparados de antemano.
- 2026-07-30: **Etapa 9 (Infraestructura): escrito y validado el Terraform/IaC real** (`infra/terraform/`, ver su propio README) — implementa las decisiones ya tomadas en `DEPLOYMENT.md` §1 (Hetzner Cloud + DigitalOcean Managed Postgres + Hetzner Object Storage + Cloudflare DNS). Antes de escribir nada se confirmó contigo: no había cuentas creadas en ningún proveedor ni dominio registrado todavía, así que el alcance de esta sesión se acotó a escribir el IaC (sin aplicar recursos reales, que exigiría tus credenciales y empezaría a costar dinero).
  - Módulos `compute` (servidor(es) Hetzner + firewall restrictivo + SSH), `database` (DO Managed Postgres + firewall por IP + usuario de aplicación), `object_storage` (bucket S3-compatible — sin recurso `hcloud_*` nativo todavía, se usa el provider `minio`, patrón documentado por el propio Hetzner), `dns` (registros Cloudflare api/app/mqtt). `dns-zone/` aparte (se aplica una sola vez, staging/producción la referencian por nombre vía data source, no por estado compartido).
  - Verificadas en vivo las versiones actuales de los 4 providers (hcloud 1.68, digitalocean 2.96/2.97, cloudflare 5.22 — mayor con cambios incompatibles desde v4, `cloudflare_record`→`cloudflare_dns_record`, sintaxis de `account`/`filter` como atributo en vez de bloque —, minio 3.33/3.38) antes de escribir nada, para no adivinar sintaxis con el conocimiento base desactualizado.
  - Instalado Terraform 1.15.8 (`winget`) en este entorno para poder validar de verdad: `terraform fmt`/`validate` en los tres directorios raíz, y `terraform plan` con tokens de formato válido pero credenciales falsas — cada proveedor real llegó a generar un plan de creación completo para sus recursos (incluida la corrección en vivo de `cloudflare_zone`/`data.cloudflare_zone` al descubrir que v5.22 exige `filter`/`account` como atributos, no bloques, algo que la documentación en texto no dejaba claro) y solo se detuvo en el paso de autenticación (esperado, sin credenciales reales). Ningún recurso real creado.
  - `DEPLOYMENT.md` actualizado (§0 estado, §12 entregables, §15 tareas, §19 historial con 3 decisiones nuevas del mismo día).
- 2026-07-30: **cerrado `BACKLOG.md` #14: un Admin de organización puede ver/revocar las sesiones de otro miembro de su equipo.** `sessions.read_others`/`revoke_others` llevaban documentados en `PERMISSIONS.md` (y probados en `permissions.spec.ts`) desde el principio, pero ningún endpoint los usaba — `AuthController`/`SessionsService` solo soportaban la sesión propia. A diferencia de `BACKLOG.md` #20 (con una justificación de negocio ya escrita en ADR-0005), aquí no había ningún documento que confirmara la necesidad, así que se preguntó antes de construir: se decidió que sí, por ser una capacidad de seguridad habitual (revocar acceso a un empleado que se va o cuya cuenta se ve comprometida) y de bajo riesgo (dentro de la propia organización).
  - Añadido `MembersService#findMemberInOrg` (resuelve `memberId` → `userId`, acotado a la organización de quien llama) y `SessionsService#listForUser`/`revokeForUser` (con `organizationId` explícito en el `where` — sin él, un Admin habría visto las sesiones de *cualquier* miembro de su organización, no solo del objetivo, ya que la política RLS de `sessions` solo distingue entre organizaciones, no entre miembros de la misma). `revokeForUser` audita la acción (`sessions.revoke_others`), a diferencia de la revocación de la sesión propia, que nunca se audita.
  - Rutas nuevas `GET`/`DELETE /members/{id}/sessions[/{sessionId}]`. De paso se corrigió `OPENAPI.yaml`: documentaba (mal) que `DELETE /auth/sessions/{sessionId}` podía revocar la sesión de otro miembro con el permiso — nunca lo hizo; corregido para reflejar que solo revoca la propia.
  - Flutter: nueva acción "Sesiones activas" por miembro en `MembersListScreen`, navega a `MemberSessionsScreen` (mismo patrón que la pantalla de sesiones propias, sin el concepto de "este dispositivo").
  - Verificado en vivo de principio a fin contra Postgres real: organización nueva, invitar un segundo miembro, aceptar invitación, iniciar sesión, y como Admin de organización listar/revocar esa sesión — confirmado `refresh` posterior → 401, doble revocación → 204 idempotente, `technician` sin permiso → 403, y la acción atribuida correctamente en `audit_log`. 46/46 unitarios + 4/4 integración RLS, `flutter analyze` limpio, 25/25 tests, `flutter build web` compila. `OPENAPI.yaml` 57→59 rutas.
- 2026-07-30: **cerrado `BACKLOG.md` #20: operaciones por ID del Admin de plataforma sobre Directorio IoT.** Tras cerrar #18 (crear+listar) surgió la pregunta de qué hacer con `PATCH`/`DELETE`/rotar-credencial: el Admin de plataforma no podía editar ni deshabilitar nada que acabara de crear, ni siquiera lo suyo (`PATCH /gateways/{id}` propio → 403). Antes de construir nada se releyó el texto original de [ADR-0005](docs/ADR/0005-admin-plataforma-gestion-global-iot.md) (2026-07-27): la Decisión **ya incluía** "actualización, baja, rotación de credencial" en el alcance, justificado porque la empresa del Admin de plataforma **gestiona** el hardware del cliente, no solo lo instala una vez — así que aceptar "solo crea" como diseño final habría revertido en silencio una decisión ya tomada, y se descartó esa opción a favor de construirlo de verdad.
  - Resultó ser un hueco más pequeño de lo que parecía: los 6 servicios de Directorio IoT (instalaciones, zonas, gateways, dispositivos, sensores, canales) **ya aceptaban** `explicitOrganizationId` en `findOne`/`update`/`disable`/`softDelete`/`rotateCredential` desde que se mecanizó #18 — solo faltaban las rutas. Añadidas `GET`/`PATCH`/`DELETE` (y `POST :id/rotate-credential` para gateways) a los 6 controladores de plataforma.
  - De paso se endureció `findOne` de zonas/dispositivos/sensores/canales con el mismo filtro `organizationId` en el `where` de Prisma ya aplicado a instalaciones/gateways en #21 — sin esto, una petición de plataforma con el `organizationId` equivocado en la URL podría haber operado sobre la fila real de otra organización (con atribución de auditoría incorrecta); verificado en vivo que ahora devuelve 404 en vez de eso.
  - Verificado en vivo el ciclo completo para los 6 recursos como Admin de plataforma puro contra Postgres real: editar instalación/zona/gateway/dispositivo, rotar credencial de gateway, deshabilitar gateway/dispositivo, eliminar zona/sensor/instalación — con `audit_log` atribuyendo cada una de las 8+ acciones a la organización correcta (confirmado por `psql` directo). `OPENAPI.yaml` ampliado 50→57 rutas (validado sintácticamente).
  - Flutter: el panel de plataforma gana los mismos botones editar/deshabilitar/rotar credencial/eliminar que ya tenía la vista de miembro (antes deliberadamente ausentes, ya que el backend los habría rechazado) — `PlatformDirectoryApi` ampliada con 9 métodos nuevos, cada uno verificado contra la ruta real antes de darlo por bueno. `flutter analyze` limpio, 25/25 tests, `flutter build web` compila.
  - 46/46 tests unitarios backend + 4/4 de integración RLS en verde (rol restringido `iot_platform_app`).
- 2026-07-30: **cerrado `BACKLOG.md` #18: ADR-0005 mecanizado por completo (Flutter)** — panel de plataforma con Directorio IoT completo por organización (`PlatformInstallationsScreen` → `PlatformInstallationDetailScreen` (zonas+gateways) → `PlatformGatewayDetailScreen` (dispositivos) → `PlatformDeviceDetailScreen` (sensores)), navegable desde una nueva acción "Directorio IoT" en el menú de cada organización (`PlatformOrganizationsScreen`). Solo crear y listar, deliberadamente sin editar/deshabilitar/rotar credencial — coherente con `BACKLOG.md` #20 (el backend rechazaría esas operaciones para un Admin de plataforma puro). Al verificar en vivo la cadena completa (organización → instalación → zona → gateway → dispositivo → sensor) contra el backend real se encontró un **bug real de aislamiento multi-tenant, no nuevo de esta entrada pero nunca antes ejercitado**: `GET /platform/organizations/{id}/installations` y `.../gateways` ignoraban por completo el `organizationId` de la URL y devolvían filas de **todas** las organizaciones (`InstallationsService#findAll`/`GatewaysService#findAll` nunca filtraban por `organizationId`, confiando solo en RLS + el alcance por instalación, que para el Admin de plataforma es siempre `'all'` — sin ningún filtro real). Corregido añadiendo `organizationId` (ya resuelto por `resolveOrgContext`, antes descartado) al `where` de `findAll`/`findOne` en ambos servicios. `BACKLOG.md` #21. Verificado de nuevo en vivo tras el fix: cada listado por organización ya solo devuelve lo suyo, y `audit_log` atribuye las 5 acciones a la organización correcta. `flutter analyze` limpio, 25/25 tests Flutter, `flutter build web` compila; 46/46 tests unitarios backend + 4/4 de integración RLS (esta vez con el rol restringido `iot_platform_app`, no el rol dueño de las tablas usado por error en el primer intento).
- 2026-07-30: **cerrado `BACKLOG.md` #18: ADR-0005 mecanizado por completo (backend)**. Hasta ahora solo gateways tenía variante de plataforma (`PlatformGatewaysController`) — un Admin de plataforma no podía crear la instalación inicial de un cliente nuevo por ninguna vía, pese a que el ADR-0005 y `API_DESIGN.md` §2 siempre documentaron que debía cubrir todo el Directorio IoT (instalaciones→canales). Añadidos `PlatformInstallationsController`/`PlatformZonesController`/`PlatformDevicesController`/`PlatformSensorsController`/`PlatformChannelsController` (este último solo lectura). Los 6 servicios (incluido `GatewaysService`) refactorizados para aceptar un `explicitOrganizationId` opcional vía el helper ya existente `resolveOrgContext`.
  - **Hallazgo adicional real**: al completar esto se descubrió que **ningún servicio de Directorio IoT auditaba nada** — ni `GatewaysService` (la propia referencia), ni para miembros normales ni para el Admin de plataforma. El requisito central de transparencia del ADR-0005 ("nunca es una acción invisible para el cliente") no se cumplía de verdad. Corregido añadiendo `auditLog.record(...)` (dentro de la misma transacción) a las acciones que mutan en los 6 servicios.
  - Verificado en vivo, de principio a fin: se creó una organización nueva completa (organización → instalación → zona → gateway → dispositivo → sensor) puramente como Admin de plataforma — algo literalmente imposible antes de este cambio — y se confirmó que el primer Admin de esa organización, tras aceptar su invitación, ve cada paso en su propia auditoría (`GET /audit-log`), correctamente atribuido al Admin de plataforma como actor. Detalle completo en [ADR-0005](docs/ADR/0005-admin-plataforma-gestion-global-iot.md#estado-de-la-implementación).
  - 46/46 tests unitarios (2 actualizados por el nuevo parámetro de `AuditLogService`) + 4/4 de integración RLS en verde. `OPENAPI.yaml` ampliado con las 5 rutas nuevas (45→50 rutas documentadas).
  - Pendiente (siguiente paso, no incluido en esta entrada): la parte de Flutter — el panel de plataforma todavía no tiene pantallas para navegar/crear el Directorio IoT de una organización cliente, solo el backend ya lo permite.
- 2026-07-30: **cerrado `BACKLOG.md` #16: umbrales por defecto de canal a nivel de organización** — `OrgChannelThresholdsController` solo tenía `PUT`, sin ningún `GET` para leer los valores actuales (un formulario que solo escribe a ciegas es mala UX, así que no se había construido la pantalla). Añadido `GET /organization/channel-thresholds` (`channels.read`, cualquier rol) y la sección correspondiente en `OrganizationSettingsScreen` — cruza el catálogo completo de `GET /channel-types` con lo ya configurado (mismo patrón que el editor de funciones de plataforma), edición solo para `org_admin`/`technician`. 25/25 tests Flutter (2 nuevos), verificado en vivo contra el backend real (catálogo de 8 tipos de canal, `PUT` seguido de `GET` confirma el valor guardado).
- 2026-07-30: **panel de plataforma en Flutter** (`PlatformOrganizationsScreen`: listar/crear/suspender/reactivar organizaciones; `PlatformOrganizationFeaturesScreen`: editor de funciones contratadas; `PlatformAuditLogScreen`) para el Admin de plataforma "puro" (sin organización propia) — hasta ahora esa persona no tenía ningún sitio a donde ir: el router siempre redirigía a `/installations`, que le habría devuelto 403 (`GET /installations` exige `organizationId`, nulo para este rol). Nuevo: el redirect de sesión distingue este caso y manda a `/platform`; un usuario que además de plataforma sea miembro de una organización llega a lo mismo desde el menú "Más". Al construir esto surgieron **tres huecos/bugs reales más**:
  - **Ningún endpoint exponía el catálogo completo de `features`** (`reports_pdf`, `campaigns`, `weather_widget`, `satellite_imagery`, `recommendations`, ya seedeados) — solo el estado por organización, vacío para cualquiera que nunca activara nada. Sin esto, el editor no tenía qué ofrecer para activar por primera vez. Corregido con `GET /platform/features` (`BACKLOG.md` #17).
  - **Bug crítico de RLS: `PUT /platform/organizations/{id}/features` rompía siempre con 500** ("new row violates row-level security policy for table audit_log"). La política de `audit_log` solo tenía una expresión (reutilizada para leer y escribir) que exige `action LIKE 'platform.%'` para que un Admin de plataforma escriba sobre otra organización — pero `org_features.update` no lleva ese prefijo. Corregido con la migración `0004_audit_log_platform_write_check` (`WITH CHECK` propio, más permisivo que `USING`) + reescribir `AuditLogService#record` para insertar sin `RETURNING` (Prisma `.create()` exige que la fila devuelta *también* pase la política de lectura, así que el `WITH CHECK` solo no bastaba). Detalle completo en `DATA_MODEL.md` §7, `BACKLOG.md` #19. Verificado en vivo: la escritura ya no falla y la organización afectada sigue viendo la entrada en su propia auditoría.
  - **Confirmado (no nuevo, ya registrado): ADR-0005 solo se mecanizó para gateways** — un Admin de plataforma no puede crear la instalación inicial de un cliente nuevo por ninguna vía; solo gestionar gateways de instalaciones ya existentes. `BACKLOG.md` #18, sin decidir todavía si se completan los 5 controladores que faltan o se ajusta la documentación a lo que realmente se construyó.
  - 23/23 tests Flutter (1 nuevo), `flutter analyze` limpio, `flutter build web` compila. Verificado en vivo contra el backend real: crear organización, suspender, reactivar, activar una función por primera vez para una organización que nunca tuvo ninguna.
- 2026-07-30: **bug crítico real encontrado en vivo: `GET /audit-log` y `GET /platform/audit-log` llevaban rotos desde siempre.** `AuditLogEntry.id` es `BigInt` en Postgres/Prisma, y `JSON.stringify` no sabe serializar `BigInt` de forma nativa — en cuanto había al menos una fila (que hay siempre, casi cualquier escritura registra auditoría), el endpoint respondía 500 ("Do not know how to serialize a BigInt"). Nunca se había detectado porque no existe ningún test para `AuditController`/`AuditService`, y nadie había llamado a este endpoint de verdad hasta construir el visor de auditoría en Flutter esta sesión. Corregido con un parche global (`main.ts`): `BigInt.prototype.toJSON` devuelve el valor como string — afecta a los tres procesos (api/ingestion/worker) por si aparece otro campo `BigInt` en el futuro, no solo a este caso. `id` pasa a viajar como string en la API (`OPENAPI.yaml` actualizado; un número de 64 bits no cabe con precisión en un `number` de JS/JSON de todos modos). Verificado en vivo contra el backend real: ambos endpoints devuelven ahora las entradas reales acumuladas durante esta sesión (invitaciones, cambios de rol, alta de organización...) en vez de 500.
- 2026-07-30: **cerrado otro hueco real de Etapa 14: ajustes de organización y auditoría** — `OrganizationSettingsScreen` (perfil: nombre/email de contacto, editable solo por `org_admin`; funciones contratadas, solo visibles para `org_admin` per `PERMISSIONS.md` §14) y `AuditLogScreen` (las 200 entradas más recientes de la organización, `audit.read_full`/`audit.read_technical`). Nuevo menú "Más" en la barra de Instalaciones (antes cada pantalla nueva añadía su propio icono; ya no escalaba) que agrupa Miembros/Organización/Auditoría según el rol. Al construir estas pantallas se encontraron y documentaron dos huecos más: la paginación por cursor documentada para telemetría/auditoría tampoco estaba implementada (`BACKLOG.md` #15, mismo patrón que #13, corregido simplificando la documentación) y `PUT /organization/channel-thresholds/{code}` no tiene ningún `GET` para leer el valor actual (`BACKLOG.md` #16, sin decidir — no se construyó esa parte de la UI a ciegas). 22/22 tests Flutter (4 nuevos), `flutter analyze` limpio, `flutter build web` compila.
- 2026-07-30: **cerrado otro hueco real de Etapa 14: sesiones activas propias** (`SessionsListScreen`, ver icono nuevo en la barra de Instalaciones) — listar y cerrar sesión en otro dispositivo (`GET`/`DELETE /auth/sessions`), disponible para cualquier rol (es sobre las sesiones de uno mismo, no de otros). Cerrar la sesión de *este* dispositivo se deshabilita en esta pantalla a propósito — ya existe el botón "Cerrar sesión" para eso, evita dos caminos confusos para la misma acción. Verificado en vivo contra el backend real (login simulando un segundo dispositivo, listar, revocar, confirmar que desaparece, revocar un ID inexistente y confirmar que sigue siendo 204 idempotente). Al construirla se encontró (no se implementó) un hueco documentado-pero-no-construido real: `sessions.read_others`/`revoke_others` están en la matriz de permisos y hasta tienen su propio test unitario, pero ningún endpoint los usa — un Admin de organización no puede en realidad ver/cerrar la sesión de otro miembro pese a que el permiso dice que sí. Registrado en `BACKLOG.md` #14 como decisión pendiente (implementar de verdad o retirar el permiso), no resuelto en silencio. 18/18 tests Flutter (2 nuevos), `flutter analyze` limpio, `flutter build web` compila.
- 2026-07-30: **primera vez que `ci-cd.yml` termina en verde de principio a fin** (run [30478519684](https://github.com/javimven/iot-platform/actions/runs/30478519684), commit `b34885d`) — `static-checks`, `unit-tests`, `integration-tests`, `build`, `dependency-scan`, `docker-image` (incluido el escaneo Trivy) y las etapas de despliegue llegan todas a éxito. Importante no sobrevender esto: `deploy-staging`, `e2e-staging`, `deploy-production` y `post-deploy-check` siguen siendo `echo` de marcador de posición (Etapa 9, nunca reemplazados por scripts reales porque no hay VPS/hosting real todavía, `DEPLOYMENT.md`) — lo que se confirma aquí es que el pipeline entero *ejecuta* sin fallos por primera vez, no que la app esté desplegada de verdad en ningún sitio. Cierra la cadena completa de bugs reales encontrados en las tres primeras ejecuciones reales de este pipeline (nunca se había disparado hasta esta sesión): RLS/migraciones (Etapa 13), `npm audit`/gitleaks (dependencias/secretos), y los dos de `infra/docker/Dockerfile` de las entradas de abajo.
- 2026-07-29: **`infra/docker/Dockerfile` nunca se había construido de verdad hasta el primer run real de `ci-cd.yml`** — el job `docker-image` siempre se saltaba porque `dependency-scan` fallaba antes (ver entradas de abajo); al arreglar eso, `docker-image` corrió por primera vez y falló con "Found 72 error(s)" de TypeScript dentro de `nest build`. Dos bugs reales, distintos, que se enmascaraban entre sí:
  - **No existía ningún `.dockerignore`** — con `context: .` (raíz del repo) en `ci-cd.yml`, `COPY apps/backend/ .` en la etapa `build` copiaba también el `node_modules` del host por encima del recién instalado dentro del contenedor. En este entorno (Windows), eso metía el motor de Prisma para Windows (`query_engine-windows.dll.node`) en una imagen `node:20-alpine` (Linux/musl) — confirmado inspeccionando la imagen ya construida. Esto **enmascaraba** el segundo bug en local: el cliente de Prisma ya generado del host (con tipos completos) tapaba el cliente roto que se generaba dentro del contenedor.
  - **`prisma/schema.prisma` no estaba disponible cuando se ejecutaba `npm ci`** en la etapa `deps`: el `postinstall` de `@prisma/client` necesita el esquema para generar el cliente, y esa etapa solo copiaba `package.json`/`package-lock.json` (por caché de capas) — sin el esquema, genera un cliente incompleto, y `nest build` falla en cascada al no resolver bien los tipos de Prisma (`Prisma.PrismaClientKnownRequestError` y similares). En un checkout limpio de verdad (GitHub Actions, sin `node_modules` previo) esto se ve directamente; en local quedaba tapado por el bug de arriba.
  - Corregido: añadido `.dockerignore` (raíz del repo, excluye `node_modules`/`dist`/`.git`/secretos); `Dockerfile` copia `apps/backend/prisma` antes de `npm ci` en la etapa `deps`, y añade un `npx prisma generate` explícito en la etapa `build` (no depender solo del `postinstall`). Verificado con un build local **totalmente limpio** (`--no-cache`, con el `.dockerignore` ya aplicando, así que sin la contaminación del host que enmascaraba el problema): compila sin errores, y el motor dentro de la imagen ya es el correcto (`libquery_engine-linux-musl.so.node`, no el `.dll` de Windows). **Confirmado con un run real de CI tras el push**: `docker/build-push-action@v5` pasó a éxito por primera vez — la imagen ya se construye y se publica en `ghcr.io` de verdad.
  - El mismo run, con el build ya arreglado, llegó por primera vez al siguiente paso: **el escaneo de la imagen con Trivy (CRITICAL/HIGH) falló** — nunca se había ejecutado hasta entonces. Sin acceso al log completo (necesita permisos de admin en el repo), se reprodujo instalando `trivy` en local y escaneando la misma imagen. Dos hallazgos reales, distintos:
    - `libssl3`/`libcrypto3` de la imagen base (`node:20-alpine`) — 1 CVE alto con parche ya publicado (`3.5.6-r0` → `3.5.7-r0`). Corregido con `apk upgrade --no-cache libssl3 libcrypto3` en la etapa `runtime`.
    - **15 altos + 1 crítico, todos dentro del propio `npm`** que trae de fábrica la imagen base (`/usr/local/lib/node_modules/npm`, sus dependencias internas como `tar`/`minimatch`/`glob`) — sin exposición real: el `ENTRYPOINT` de la imagen final es `node dist/main.js` directo, `npm` nunca se ejecuta en producción, es peso muerto heredado de la imagen base. Corregido eliminándolo de la etapa `runtime` (`npm`/`npx`/`corepack`) en vez de dejarlo como vulnerabilidad sin uso real.
    - Verificado en local con el mismo binario de `trivy` y la misma configuración exacta del workflow (`--severity CRITICAL,HIGH --exit-code 1`): pasa a `0` hallazgos, `node` sigue funcionando con normalidad tras quitar `npm`.
- 2026-07-29: **cerrado otro hueco real de Etapa 14: gestión de miembros** (listar, invitar, cambiar rol, suspender/reactivar, asignar alcance por instalación, eliminar) — el backend siempre tuvo `/members*` completo (Etapa 13c) pero Flutter no tenía pantalla, así que un `org_admin` no podía hacer crecer su equipo desde la app (justo lo que el fix de `accept-invitation` de más abajo dejaba a medias: se puede *aceptar* una invitación desde la app, pero hasta ahora nadie podía *enviarla* desde ahí). Añadida `MembersListScreen` + `MembersApi`/`Member` (aplanado, sin `passwordHash` — ver el bug de abajo) + ruta `/members`, con entrada visible en la barra de Instalaciones solo para `org_admin`. Añadido el verbo `PUT` a `ApiClient` (no existía ninguno hasta ahora, lo necesita `PUT /members/:id/scope`). Al verificar en vivo contra el backend real (crear invitación, listar, cambiar rol/estado, asignar alcance, eliminar — con curl replicando exactamente el contrato de `MembersApi`) surgió el bug crítico de la entrada de abajo, y de paso un segundo bug menor: ni `invite()` ni `updateRoleOrStatus()` devolvían `email`/`fullName` (no filtraban el hash, pero tampoco cumplían el esquema `Member` documentado) — habría roto el parseo del modelo en Flutter con una excepción de tipo justo después de invitar/editar a alguien con éxito. Corregido igual que `listForOrganization` (`invite()` reutiliza el `targetUser` ya cargado en la misma transacción, sin consulta extra). 16/16 tests Flutter (4 nuevos), `flutter analyze` limpio, `flutter build web` compila.
- 2026-07-29: **bug crítico de seguridad real encontrado en vivo: `GET /members` filtraba el hash Argon2 de la contraseña de cada miembro** al org_admin que listara su equipo. Causa: `MembersService#listForOrganization` hacía `include: { user: true }` (Prisma) y devolvía el objeto crudo tal cual como respuesta HTTP, sin ningún filtrado de campos — `user.passwordHash` viajaba completo al cliente. Encontrado al construir la pantalla de Miembros en Flutter: la respuesta real ni coincidía con el esquema `Member` de `OPENAPI.yaml` (`user` anidado en vez de `email`/`fullName` planos), y al inspeccionar ese `user` anidado apareció el hash. Corregido con `include: { user: { select: { email, fullName } } }` (Prisma nunca llega a pedir `passwordHash`, no es solo un filtro posterior) — verificado en vivo que la respuesta ya no incluye el hash ni el objeto anidado, y coincide con el esquema documentado. Revisado el resto del código por el mismo patrón: solo otro uso (`notification-dispatch.service.ts`), interno, nunca serializado en una respuesta HTTP. Detalle completo en `SECURITY.md` §7.
- 2026-07-29: **cerrado un hueco real de Etapa 14**: el backend siempre tuvo `POST /auth/accept-invitation`, `/auth/forgot-password` y `/auth/reset-password` (Etapa 13i), pero Flutter no tenía ninguna pantalla para ellos — la única forma de activar una cuenta invitada o recuperar una contraseña era leer Mailpit y llamar a la API a mano, como se hizo en las pruebas de esta sesión. Sin esto, la invitación (el único mecanismo real de alta de usuarios de la plataforma) no era usable desde la app de verdad. Añadidas 3 pantallas (`AcceptInvitationScreen`, `ForgotPasswordScreen`, `ResetPasswordScreen`) + 3 métodos en `AuthApi`, con rutas nuevas en `app_router.dart` (`/accept-invitation`, `/forgot-password`, `/reset-password`) accesibles independientemente del estado de sesión (para no perder el `token` de la URL durante el `initial` del bootstrap). Enlace "¿Olvidaste tu contraseña?" añadido al login.
  - Sin herramienta de automatización de navegador en este entorno, no se pudo interactuar visualmente con la UI. En su lugar se verificó el contrato HTTP exacto que usa el nuevo `AuthApi` (mismo endpoint, mismo cuerpo JSON) contra el backend real: se creó una organización de prueba de verdad, se leyó el token de invitación real desde Mailpit, se activó la cuenta, se inició sesión, se solicitó y leyó un restablecimiento de contraseña real, y se confirmó que la contraseña antigua deja de funcionar y la nueva sí. `flutter analyze` limpio, 12/12 tests, `flutter build web` compila.
  - Efecto colateral encontrado y descartado: al probar esto se reprodujo el error `secretOrPrivateKey must be an asymmetric key when using RS256` que ya se había corregido el 2026-07-28 — pero esta vez la causa no era el código, era mi propio método de arrancar el servidor (`source .env` en bash antes de `node dist/main.js`): bash no expande el `\n` de `JWT_PRIVATE_KEY="...\n...\n..."` como sí hace `dotenv` internamente, y al quedar la variable ya puesta en `process.env`, `dotenv` (que no sobrescribe variables ya existentes) nunca llega a corregirla. Arrancar sin pre-cargar `.env` en el shell (dejando que `ConfigModule.forRoot()` lo cargue él mismo) lo resuelve — no fue necesario ningún cambio de código.
- 2026-07-29: con `npm audit` ya en verde, el mismo job `dependency-scan` llegó por primera vez a su segundo paso (`gitleaks`) y falló: falso positivo real en `apps/backend/.env.example` (el placeholder de la clave RSA, `"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"` con puntos suspensivos literales, dispara la regla `private-key` de gitleaks por tener las cabeceras PEM aunque no haya contenido real). No se pudo leer el log del job vía la API pública de GitHub (requiere permisos de administrador incluso en un repo público) — reproducido en local descargando el binario de `gitleaks` directamente (no hay `gh` ni `gitleaks` instalados en este entorno) y ejecutándolo contra el repo real. Corregido con `.gitleaks.toml` (nuevo, raíz del repo) usando una excepción por **regex** del patrón exacto del placeholder, no por ruta de fichero — verificado con una prueba aparte (repo de prueba con una clave con pinta real en la misma ruta) que la excepción no exime todo el fichero, solo el placeholder literal. Ver `SECURITY.md` §7.
- 2026-07-28: **primera ejecución real de `ci-cd.yml` en GitHub Actions** (tras el fix de rama `main`→`master` y el commit de los hallazgos de RLS) — confirmó en vivo que el paso nuevo de aprovisionar el rol restringido en el Postgres efímero de CI funciona (`integration-tests`: éxito), pero `dependency-scan` falló: `npm audit --audit-level=high` encontró 56 vulnerabilidades (40 high) en el backend, nunca detectadas antes porque el pipeline nunca se había disparado. Investigado y resuelto:
  - **`nodemailer` (directo, con exposición real: `MailerService` lo usa para SMTP)** tenía CVEs de inyección SMTP/CRLF, bypass de validación TLS y SSRF sin parche dentro de la serie 6.x — subido a `9.0.3` (única versión sin estos CVEs), verificado con un envío real de correo a Mailpit (no solo `tsc`/lint) para confirmar que la superficie de API usada (`createTransport`/`sendMail` con host/port/auth) se mantuvo estable pese al salto de 3 versiones mayores. Sin cambios de código.
  - **`tmp`/`webpack`/`picomatch`/`inquirer` (vía `@nestjs/cli`, 33 de los 40 "high")**: nunca se ejecutan en el proceso desplegado, solo en local durante `nest build`/generación de código — `dependency-scan` ahora audita con `--omit=dev` (`ci-cd.yml`) para que la puerta refleje riesgo real de runtime, no ruido de tooling.
  - **`lodash` (vía `@nestjs/config`), `multer` (vía `@nestjs/platform-express`, confirmado no alcanzable — no hay ninguna ruta de subida de ficheros implementada) y `qs`/`body-parser` (sí alcanzable en cada petición)**: la primera pasada asumió que solo se resolvían con una subida mayor de NestJS v10→v11, pero investigando más a fondo cada uno tenía una versión parcheada ya publicada (`lodash@4.18.1`, `multer@2.2.0`, `qs@6.15.3`) que `@nestjs/cli`/`@nestjs/config`/`@nestjs/platform-express` simplemente no habían adoptado todavía en sus rangos declarados. Resuelto sin tocar NestJS: forzadas las tres vía el campo `overrides` de `package.json`. Verificado con la API real arrancada y peticiones HTTP reales (JSON body + query strings con sintaxis de array/objeto, el patrón exacto de los CVEs de `qs`), además de `tsc`/`eslint`/`nest build`/46 unitarios/4 de integración.
  - Con `--omit=dev` + los tres `overrides`, `npm audit --audit-level=high` pasa con **0 vulnerabilidades altas/críticas** (exit code 0) — quedan 6 moderadas/bajas (`ajv`, `file-type`, `body-parser` residual) sin fix no-breaking disponible aún, registradas en `SECURITY.md` §10 como riesgo conocido no bloqueante.
- 2026-07-28: **Docker Desktop instalado y stack completo levantado por primera vez en este entorno** (`docker-compose.yml`: Postgres/Redis/EMQX/Mailpit/OTel collector). Requirió antes habilitar WSL2 (Windows 10 Home no tiene Hyper-V) — dos comandos DISM en PowerShell elevada + reinicio, hechos por el usuario. Esto abrió la puerta a validar, por primera vez, cosas que hasta ahora solo se habían revisado en código o probado con Prisma mockeado:
  - **`0001_init` (creación de las tablas base) faltaba del repositorio** — `0002`/`0003` siempre asumieron su existencia pero nunca se había podido generar sin un Postgres vivo. Generada con `prisma migrate diff --from-empty` y editada a mano para excluir las 4 tablas que `0003` crea particionadas. Ver `DATA_MODEL.md` §8.
  - **RLS estaba completamente sin efecto desde que existe este proyecto**: `docker-compose.yml` conectaba `api`/`ingestion`/`worker` con el mismo rol (`iot_platform`) que es dueño de las tablas y superusuor de arranque de la imagen oficial de Postgres — un superusuario/dueño se salta cualquier política de RLS por diseño, con o sin `ENABLE ROW LEVEL SECURITY`. Verificado en vivo: dos organizaciones de prueba creadas por la API veían mutuamente todas sus instalaciones. Corregido con un rol de aplicación separado sin `BYPASSRLS`/superusuario, creado automáticamente al inicializar el volumen (`infra/docker/postgres-init/01-app-role.sql`, vía `docker-entrypoint-initdb.d`). Ver `DATA_MODEL.md` §7/§9.
  - **Incluso con el rol correcto, cualquier login rompía con 500**: `PrismaService.runInTenantContext` fijaba `app.current_org_id`/`app.current_user_id` a `''` cuando no había valor (login antes de elegir organización) — y aunque se corrigiera a pasar `null`, `set_config(name, NULL, true)` **tampoco guarda `NULL` real**, Postgres lo convierte en `''`, así que `''::uuid` seguía rompiendo. Corregido con `NULLIF(current_setting(...), '')::uuid` en las 9 políticas RLS afectadas (`0002`, `0003`). La suposición original de `DATA_MODEL.md` §9 ("es un fallo seguro, no se ve nada") era incorrecta en los dos frentes — corregida in situ.
  - Añadida `apps/backend/test/integration/rls-isolation.spec.ts` — primera prueba de integración real del proyecto (contra Postgres real, no mocks), cubre exactamente los 3 hallazgos de arriba. `ci-cd.yml` actualizado para crear el mismo rol restringido en el Postgres efímero de CI (antes usaba el rol dueño, mismo problema) — **sin confirmar todavía con un run real de GitHub Actions** (el pipeline nunca se ha disparado; de paso se corrigió que escuchaba en `main` en vez de `master`, el branch real de este repo).
  - Validado end-to-end con dos organizaciones reales: alta vía `POST /platform/organizations`, invitación por email real capturada en Mailpit, `accept-invitation`, login, aislamiento confirmado (`GET /installations` solo devuelve lo propio), acceso cruzado por ID → 404 (no 403, `PERMISSIONS.md` §9/§13). 46 tests unitarios + 4 de integración, `tsc`/`nest build`/`eslint`/`prettier` todos en verde tras los fixes.
  - Efecto colateral: se necesitó separar `tsconfig.eslint.json` (incluye `test/`) del `tsconfig.json` de producción (solo `src/`) — incluir `test/` en el mismo tsconfig rompía la estructura de `dist/` (`dist/src/main.js` en vez de `dist/main.js`).
- 2026-07-28: repositorio publicado en GitHub (`github.com/javimven/iot-platform`, público — minutos de Actions gratis e ilimitados incluso en runners macOS). Se evaluó `MobAI-App/ios-builder` (herramienta de terceros que usa runners macOS de GitHub Actions para compilar iOS desde Windows) pero se descartó a favor de un workflow propio (`mobile-ios-build.yml`): mismo mecanismo subyacente (runner `macos-14` de GitHub Actions), sin depender de un binario de terceros con acceso a la cuenta de GitHub ni de un segundo servicio ("MobAI") no evaluado. El workflow compila `apps/mobile` para iOS sin firma de código (`flutter build ios --release --no-codesign`, no hay cuenta de Apple Developer) — confirma que el código compila para iOS, no genera un `.ipa` instalable. Se dispara en push a `apps/mobile/**` en `master` o manualmente (`workflow_dispatch`). Cierra la única limitación que quedaba sin validar de los tres targets (web/Android/iOS).
- 2026-07-28: validado por primera vez `flutter build apk --debug` (hasta ahora solo se había probado `flutter build web`). Falló al primer intento: `jni` 1.0.1 (dependencia transitiva de `flutter_secure_storage` → `path_provider_android`) está **retirado por sus propios autores en pub.dev** — su `build.gradle` solo aplicaba el plugin de Kotlin si AGP < 9, y este proyecto usa AGP 9.0.1 (generado por `flutter create` en 2026), así que fallaba con "Could not find method kotlin()". Corregido con `dependency_overrides: jni: ^1.0.2` en `apps/mobile/pubspec.yaml` (esa versión elimina el bloque de Kotlin que ya no hacía falta) — a quitar en cuanto `path_provider_android` deje de depender de la 1.0.1. `flutter analyze`/`test`/`build web`/`build apk --debug` todos verdes tras el fix.
- 2026-07-27: cerrado el último hueco del Directorio IoT en Flutter — reasignar la zona de un dispositivo. `DeviceDetailScreen` resuelve la instalación del dispositivo vía `GET /zones/:id` (la zona ya trae `installationId`) antes de pedir las zonas de esa instalación para el desplegable de edición. Añadidos `zoneId` a `Device` e `installationId` a `Zone` (Dart) y `DirectoryApi.getZone()`. `flutter analyze` limpio, 12/12 tests, `flutter build web` compila.
- 2026-07-27: añadida edición/deshabilitar de zona/gateway/dispositivo y baja de sensor al Directorio IoT en Flutter (antes solo alta + edición de umbral de canal), completando el CRUD salvo reasignar zona de un dispositivo (requeriría que `DeviceDetailScreen` conociera la instalación del dispositivo, no disponible hoy solo con `deviceId`). Añadido `ApiClient.delete()` (no existía ningún verbo DELETE en el cliente HTTP hasta ahora). Diálogo de confirmación compartido (`showConfirmDialog`) reutilizado en las 4 bajas/deshabilitaciones. `flutter analyze` limpio, 12/12 tests, `flutter build web` compila.
- 2026-07-27: añadida alta de zona/gateway/dispositivo/sensor y edición de umbral de canal al Directorio IoT en Flutter (antes solo lectura). Diálogo compartido `showGatewayCredentialDialog` para mostrar la credencial una única vez (al crear y al rotar), con copiar-al-portapapeles y `barrierDismissible: false`. Botones de alta ocultos client-side para `operator`/`read_only` (pista de UI, no control de acceso). `flutter analyze` limpio, 12/12 tests, `flutter build web` compila.
- 2026-07-27: resuelta la decisión pendiente de `BACKLOG.md` #13 (paginación offset documentada pero nunca implementada). Se eligió la opción conservadora: simplificar `API_DESIGN.md` §5 y `OPENAPI.yaml` para reflejar el array plano que el backend realmente devuelve (instalaciones/zonas/gateways/dispositivos/sensores/canales/miembros/alertas), en vez de retrofitar paginación real en ~7 endpoints sin nadie disponible para revisar un cambio de ese tamaño. Eliminados los esquemas `MemberPage`/`InstallationPage`/`GatewayPage`/`AlertPage`/`PageMeta` y los parámetros `Page`/`PageSize`, ya huérfanos. Solo documentación — sin cambios de código; `OPENAPI.yaml` validado sintácticamente (43 rutas, 31 esquemas).
- 2026-07-27: construido el Directorio IoT de solo lectura en Flutter (`apps/mobile/lib/features/directory/`): instalación → zonas + gateways → dispositivos → sensores → canales (con umbral de alerta si tiene uno propio). Deliberadamente sin alta/edición/baja en esta etapa — cinco entidades × crear/editar/borrar es alcance propio, no se mezcla con la vista de solo lectura. `GET /gateways` no filtra por instalación (backend) — se filtra en cliente, aceptable a la escala del MVP, mismo gap ya registrado en `BACKLOG.md` #13. `flutter analyze` limpio, 11/11 tests, `flutter build web` compila.
- 2026-07-27: construida la gráfica histórica por canal en Flutter (`apps/mobile/lib/features/readings/`) usando `fl_chart` (nueva dependencia, justificada en `apps/mobile/README.md` — librería de UI, no arquitectura, no necesita ADR). Presets 24h (crudo) / 7 días (horario) / 30 días (diario) contra `GET /channels/:channelId/readings`, con banda mín/máx cuando el backend la devuelve (canales de tipo media). Se navega desde `InstallationDetailScreen` pulsando una lectura. `flutter analyze` limpio, 8/8 tests, `flutter build web` compila.
- 2026-07-27: construida la pantalla de Alertas en Flutter (`apps/mobile/lib/features/alerts/`) contra el backend real, con dos correcciones al conectar: (1) `AlertsService.findAll()` (backend) ahora enriquece cada alerta con `installationName`/`channelTypeCode`/`deviceName`/`gatewayName` (aditivo sobre `Alert`, `OPENAPI.yaml`) — sin esto la lista solo tendría UUIDs en crudo; (2) se detectó que `GET /installations` devuelve un array plano y no el sobre `{data, meta}` que tanto `OPENAPI.yaml` como `installations_api.dart` asumían — bug real que habría lanzado un `TypeError` en producción, corregido en el cliente; el gap de fondo (paginación documentada en `API_DESIGN.md` §5 pero no implementada en ~7 endpoints) queda registrado en `BACKLOG.md` #13 como decisión pendiente, no resuelto silenciosamente. También se corrigió que `ReadingsService.getLatest*` no devolvía `channelTypeCode` pese a estar documentado y ya esperado por el cliente. Backend: 46/46 tests. Flutter: `flutter analyze` limpio, 6/6 tests, `flutter build web` compila.
- 2026-07-27: instalado Flutter 3.44.8 estable en este entorno (SDK ya presente en `C:\src\flutter`, no fue necesario clonarlo de nuevo; Android SDK/Android Studio ya estaban instalados, solo faltó fijar `JAVA_HOME` al JDK incluido en Android Studio). Se ejecutó `flutter create --platforms=android,ios,web .` sobre `apps/mobile` (sin repositorio git en ese momento, se hizo copia de seguridad manual antes) — confirmado por diff que no tocó ningún archivo escrito a mano (`pubspec.yaml`, `analysis_options.yaml`, `lib/`); solo añadió las carpetas de plataforma esperadas y un `test/widget_test.dart` de plantilla (contador por defecto, sin relación con esta app) que se eliminó. `flutter analyze` sin incidencias, `flutter test` 4/4, `flutter build web` compila. Cierra la incertidumbre de Etapa 14 sobre código Flutter no verificado.
- 2026-07-27: creado [`docs/MAINTENANCE.md`](docs/MAINTENANCE.md) (Etapa 15, cierra el proceso 0-15). No añade decisiones nuevas de arquitectura — consolida cadencias y umbrales de escala ya dispersos en `SECURITY.md`, `DATA_MODEL.md`, `NON_FUNCTIONAL_REQUIREMENTS.md` y ADR-0003, y añade una cadencia trimestral de revisión de coste/escala que no existía en ningún documento anterior. `SECURITY.md` §9 actualizado para enlazar aquí en vez de a una etapa futura sin nombre.
- 2026-07-27: creado [`docs/PRIVACY.md`](docs/PRIVACY.md) — documento transversal (RGPD) que `NON_FUNCTIONAL_REQUIREMENTS.md` §13 e `INCIDENT_RESPONSE.md` referenciaban desde su creación como "a redactar". Inventario de datos personales, roles responsable/encargado del tratamiento (**pendiente de tu validación legal**, no solo técnica), derechos de los interesados, procedimiento de notificación de brechas (72h AEPD) y subencargados (Hetzner/DO/SMTP/observabilidad). Detecta dos gaps reales de producto: no hay anonimización de usuario al ejercer derecho de supresión, y no hay endpoint de autoservicio de acceso a datos — documentados como deuda MVP conocida, no bloqueantes.
- 2026-07-27: Notificaciones (Etapa 13i) — al conectar `MailerService` a `PlatformOrganizationsService` faltó registrarlo también como provider de `PlatformModule` (ya estaba en `IamModule`), lo que habría causado un fallo de resolución de dependencias de NestJS en arranque; corregido. `auth.service.spec.ts` quedó desactualizado tras añadir `MailerService`/`ConfigService` al constructor de `AuthService` (forgot/reset-password) — actualizado con los mocks correspondientes. Añadido `APP_BASE_URL` a `.env.example` (usado por los tres servicios que generan enlaces de email).
- 2026-07-27: soporte de estaciones NB-IoT/conexión directa (sin concentrador) — modeladas como "gateway de un solo dispositivo" ([ADR-0004](docs/ADR/0004-unidad-de-conexion-lora-y-nbiot.md)). Afectó a `ARCHITECTURE.md`, `FUNCTIONAL_REQUIREMENTS.md`, `PRODUCT_REQUIREMENTS.md` y `DATA_MODEL.md`.
- 2026-07-27: volcado de ideas del usuario ([`BACKLOG.md`](docs/BACKLOG.md)). Aplicado ya en MVP: agregación de gráficas por tipo de canal (media/acumulado), coordenadas GPS en instalaciones, feature flags por organización (`features`/`organization_features`), y una excepción explícita y auditada para que el Admin de plataforma gestione Directorio IoT de cualquier organización sin ser miembro ([ADR-0005](docs/ADR/0005-admin-plataforma-gestion-global-iot.md)). Idioma confirmado como autoservicio (ES/EN/FR), V2. Resto de ideas (informes PDF, clima, campañas, satélite, enfermedades, recomendaciones, agente IA) clasificadas V2/Futuro en el backlog.
